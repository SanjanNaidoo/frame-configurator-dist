// GENERATED by configurator/build.mjs from configurator/lib and configurator/ui. Do not edit: change the source and rebuild.

(() => {
'use strict';
const STYLES = "/* Picframing configurator — warm, print-shop feel. Framebridge-adjacent, not a clone.\n   Lives inside the element's Shadow DOM: nothing here reaches the Wix page, and\n   the page's selectors can't reach in. Inherited properties (font, colour) still\n   flow through the host, which is why .fc sets all of them explicitly. */\n\n:host {\n  display: block;\n  --cream:      #FDF6EF;\n  --cream-deep: #F5EADF;\n  --ink:        #1E1B18;\n  --ink-soft:   #55504A;\n  --ink-faint:  #8B837A;\n  --rule:       #E2D5C6;\n  --accent:     #3F5E77;\n  --accent-ink: #2C4356;\n  --warn-bg:    #FBF0DC;\n  --warn-ink:   #7A5A17;\n  --err-bg:     #F9E7E4;\n  --err-ink:    #8A3A2E;\n  --ok-bg:      #E6EFE4;\n  --ok-ink:     #2E5A2B;\n  --serif: \"Iowan Old Style\", \"Palatino Linotype\", Palatino, Georgia, serif;\n  --sans: -apple-system, BlinkMacSystemFont, \"Segoe UI\", Inter, Helvetica, Arial, sans-serif;\n}\n\n*, *::before, *::after { box-sizing: border-box; }\n/* Must beat the display:flex/grid rules below, or hidden panels still show. */\n[hidden] { display: none !important; }\n/* Everything inherits from here, not from the Wix page around us. */\n.fc {\n  container: fc / inline-size;\n  background: var(--cream);\n  color: var(--ink);\n  font-family: var(--sans);\n  font-size: 15px;\n  font-weight: 400;\n  line-height: 1.55;\n  letter-spacing: normal;\n  text-align: left;\n  -webkit-font-smoothing: antialiased;\n}\n\n.wrap { max-width: 1180px; margin: 0 auto; padding: 48px 28px 80px; }\n.wrap--narrow { max-width: 640px; }\n\n.display {\n  font-family: var(--serif);\n  font-weight: 700;\n  font-size: clamp(30px, 4.2cqi, 46px);\n  line-height: 1.12;\n  letter-spacing: -0.01em;\n  margin: 0 0 32px;\n  text-align: center;\n}\n.display--sm { font-size: clamp(24px, 3cqi, 30px); text-align: left; margin: 4px 0 28px; }\n.lede { font-size: 17px; color: var(--ink-soft); margin: 0 0 28px; }\n\n.banner {\n  background: var(--warn-bg);\n  color: var(--warn-ink);\n  padding: 10px 20px;\n  font-size: 13px;\n  text-align: center;\n  border-bottom: 1px solid #E8D9B4;\n}\n.banner strong { font-weight: 700; }\n.banner span { opacity: 0.85; }\n\n/* ---------------- router ---------------- */\n.router-cols {\n  display: grid;\n  grid-template-columns: 1fr 1fr 260px;\n  gap: 40px;\n  margin-bottom: 34px;\n  align-items: start;\n}\n.router-head { font-family: var(--serif); font-size: 21px; margin: 0 0 6px; font-weight: 700; }\n.router-blurb { margin: 0; color: var(--ink-soft); font-size: 14px; }\n.router-col--aside { font-size: 14px; color: var(--ink-soft); }\n.aside { margin: 0 0 4px; }\n.pin { color: var(--accent); font-size: 10px; vertical-align: 2px; margin-right: 6px; }\n\n.tiles { display: grid; grid-template-columns: repeat(auto-fit, minmax(190px, 1fr)); gap: 18px; }\n.tiles--muted { margin-top: 18px; opacity: 0.92; }\n\n.tile {\n  border: 1px solid var(--rule);\n  background: #fff;\n  padding: 0;\n  text-align: left;\n  cursor: pointer;\n  font: inherit;\n  color: inherit;\n  transition: border-color .15s, transform .15s, box-shadow .15s;\n  display: flex;\n  flex-direction: column;\n}\n.tile:hover { border-color: var(--accent); transform: translateY(-2px); box-shadow: 0 6px 20px rgba(30,27,24,.08); }\n.tile-art { aspect-ratio: 4/3.4; background: var(--cream-deep); position: relative; overflow: hidden; }\n.tile-art::after {\n  content: \"\"; position: absolute; inset: 18%;\n  border: 3px solid rgba(30,27,24,.16);\n  background: repeating-linear-gradient(135deg, rgba(30,27,24,.05) 0 8px, transparent 8px 16px);\n}\n.tile-art, .tile-body, .tile-name, .tile-blurb, .tile-flag { display: block; }\n.tile-body { padding: 14px 16px 18px; }\n.tile-name { font-family: var(--serif); font-size: 17px; font-weight: 700; margin: 0 0 4px; }\n.tile--quotable .tile-name { color: var(--accent-ink); }\n.tile-blurb { margin: 0; font-size: 13px; color: var(--ink-soft); line-height: 1.45; }\n.tile-flag { display: inline-block; font-size: 11px; letter-spacing: .06em; text-transform: uppercase; color: var(--ink-faint); margin-top: 8px; }\n\n.router-foot { text-align: center; margin-top: 40px; color: var(--ink-soft); }\n.link { color: var(--accent-ink); text-decoration: underline; text-underline-offset: 3px; }\n.linkbtn { background: none; border: 0; padding: 0; font: inherit; color: var(--accent-ink); text-decoration: underline; text-underline-offset: 3px; cursor: pointer; }\n.back-link { background: none; border: 0; padding: 0; font: inherit; font-size: 13px; color: var(--ink-faint); cursor: pointer; margin-bottom: 14px; }\n.back-link:hover { color: var(--ink); }\n\n/* ---------------- config layout ---------------- */\n.config-shell {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) 400px;\n  gap: 56px;\n  max-width: 1180px;\n  margin: 0 auto;\n  padding: 40px 28px 96px;\n  align-items: start;\n}\n.steps { min-width: 0; }\n.step { border-top: 1px solid var(--rule); padding: 26px 0 6px; }\n.step:first-of-type { border-top: 0; padding-top: 0; }\n.step-head { display: flex; align-items: baseline; gap: 12px; margin-bottom: 14px; }\n.step-num {\n  font-family: var(--serif); font-size: 13px; width: 24px; height: 24px;\n  border: 1px solid var(--rule); border-radius: 50%;\n  display: grid; place-items: center; color: var(--ink-faint); flex: none;\n}\n.step-head h2 { font-family: var(--serif); font-size: 20px; margin: 0; font-weight: 700; }\n.step-note { margin: -6px 0 14px 36px; font-size: 13px; color: var(--ink-soft); }\n.sub-label { font-size: 12px; letter-spacing: .07em; text-transform: uppercase; color: var(--ink-faint); margin: 22px 0 8px; }\n\n/* dropzone */\n.dropzone {\n  border: 1.5px dashed var(--rule); background: #fff;\n  padding: 40px 24px; text-align: center; cursor: pointer;\n  transition: border-color .15s, background .15s;\n}\n.dropzone:hover, .dropzone.is-over { border-color: var(--accent); background: #fff; }\n.dz-icon { font-size: 22px; color: var(--accent); margin-bottom: 8px; }\n.dz-title { margin: 0 0 4px; font-size: 15px; }\n.dz-sub { margin: 0; font-size: 13px; color: var(--ink-faint); }\n\n.filemeta { display: flex; gap: 16px; align-items: center; background: #fff; border: 1px solid var(--rule); padding: 12px; }\n.filemeta img { width: 66px; height: 66px; object-fit: cover; flex: none; background: var(--cream-deep); }\n.fm-name { margin: 0 0 2px; font-size: 14px; font-weight: 600; word-break: break-all; }\n.fm-dims { margin: 0 0 4px; font-size: 13px; color: var(--ink-soft); }\n\n/* option grids */\n.opt-grid { display: grid; gap: 10px; }\n.opt-grid--sizes { grid-template-columns: repeat(auto-fill, minmax(112px, 1fr)); }\n.opt-grid--swatch { grid-template-columns: repeat(auto-fill, minmax(96px, 1fr)); }\n\n.opt {\n  border: 1px solid var(--rule); background: #fff; cursor: pointer;\n  font: inherit; color: inherit; text-align: left; padding: 10px 12px;\n  transition: border-color .12s, box-shadow .12s;\n}\n.opt:hover { border-color: var(--ink-faint); }\n.opt[aria-pressed=\"true\"] { border-color: var(--accent); box-shadow: inset 0 0 0 1px var(--accent); }\n.opt:disabled { opacity: .38; cursor: not-allowed; }\n.opt-name { display: block; font-size: 13px; font-weight: 600; }\n.opt-meta { display: block; font-size: 11.5px; color: var(--ink-faint); margin-top: 2px; }\n.opt-meta--warn { color: var(--warn-ink); }\n\n.swatch { width: 100%; aspect-ratio: 2.6/1; border: 1px solid rgba(30,27,24,.14); margin-bottom: 7px; display: block; }\n\n/* option list rows */\n.opt-list { display: flex; flex-direction: column; gap: 8px; }\n.opt-row {\n  display: flex; justify-content: space-between; align-items: flex-start; gap: 14px;\n  border: 1px solid var(--rule); background: #fff; padding: 11px 14px;\n  cursor: pointer; font: inherit; color: inherit; text-align: left; width: 100%;\n  transition: border-color .12s, box-shadow .12s;\n}\n.opt-row:hover { border-color: var(--ink-faint); }\n.opt-row[aria-pressed=\"true\"] { border-color: var(--accent); box-shadow: inset 0 0 0 1px var(--accent); }\n.opt-row-main { min-width: 0; }\n.opt-row-name { display: block; font-size: 14px; font-weight: 600; }\n.opt-row-desc { display: block; font-size: 12.5px; color: var(--ink-soft); margin-top: 2px; }\n.opt-row-price { font-size: 13px; color: var(--ink-faint); white-space: nowrap; flex: none; }\n\n/* segmented */\n.seg { display: inline-flex; border: 1px solid var(--rule); background: #fff; margin-bottom: 16px; }\n.seg button {\n  border: 0; background: none; font: inherit; font-size: 13.5px; padding: 9px 18px;\n  cursor: pointer; color: var(--ink-soft); border-right: 1px solid var(--rule);\n}\n.seg button:last-child { border-right: 0; }\n.seg button[aria-pressed=\"true\"] { background: var(--accent); color: #fff; font-weight: 600; }\n\n.slider-row { display: flex; align-items: center; gap: 14px; margin: 20px 0 6px; font-size: 13.5px; }\n.slider-row label { color: var(--ink-soft); flex: none; }\n.slider-row input { flex: 1; accent-color: var(--accent); }\n.slider-row output { font-variant-numeric: tabular-nums; color: var(--ink); flex: none; width: 56px; }\n\n.check { display: flex; gap: 9px; align-items: flex-start; font-size: 13.5px; margin-top: 12px; cursor: pointer; }\n.check input { margin-top: 3px; accent-color: var(--accent); }\n.check em { color: var(--ink-faint); font-style: normal; }\n\n/* ---------------- preview ---------------- */\n.panel-sticky { position: sticky; top: 28px; }\n.preview-stage {\n  background: var(--cream-deep);\n  border: 1px solid var(--rule);\n  height: 340px;\n  display: grid; place-items: center;\n  padding: 24px;\n  overflow: hidden;\n}\n.pv-frame { position: relative; box-shadow: 0 10px 30px rgba(30,27,24,.22); background: #C9A227; }\n.pv-mat, .pv-mat2, .pv-art { position: absolute; }\n.pv-mat { background: #F2ECDD; box-shadow: inset 0 0 0 1px rgba(0,0,0,.06); }\n.pv-mat2 { background: #F2ECDD; }\n.pv-art {\n  background-color: #D8D2C9;\n  background-repeat: no-repeat;\n  box-shadow: 0 0 0 1px rgba(0,0,0,.10), 2px 2px 6px rgba(0,0,0,.14);\n  display: grid; place-items: center;\n}\n.pv-empty { font-size: 11px; color: rgba(30,27,24,.4); letter-spacing: .04em; }\n\n.pv-art.is-croppable { cursor: grab; touch-action: none; }\n.pv-art.is-dragging { cursor: grabbing; }\n\n.crop-controls { margin-top: 12px; }\n.crop-hint { margin: 0 0 6px; font-size: 12px; color: var(--ink-faint); text-align: center; }\n.crop-row { display: flex; align-items: center; gap: 12px; font-size: 13px; }\n.crop-row label { color: var(--ink-soft); flex: none; }\n.crop-row input { flex: 1; accent-color: var(--accent); }\n\n.dims-caption { text-align: center; font-size: 12.5px; color: var(--ink-faint); margin: 10px 0 22px; font-variant-numeric: tabular-nums; }\n\n/* ---------------- quote ---------------- */\n.quote { border: 1px solid var(--rule); background: #fff; padding: 20px; }\n.quote-head { display: flex; justify-content: space-between; align-items: baseline; gap: 12px; }\n.quote-label { font-size: 12px; letter-spacing: .07em; text-transform: uppercase; color: var(--ink-faint); }\n.quote-amount { font-family: var(--serif); font-size: 26px; font-weight: 700; font-variant-numeric: tabular-nums; }\n.quote-sub { margin: 4px 0 0; font-size: 12.5px; color: var(--ink-faint); }\n\n.alerts { display: flex; flex-direction: column; gap: 8px; margin-top: 14px; }\n.alert { font-size: 13px; padding: 10px 12px; line-height: 1.45; }\n.alert--warn { background: var(--warn-bg); color: var(--warn-ink); }\n.alert--err { background: var(--err-bg); color: var(--err-ink); }\n\n.breakdown-toggle {\n  background: none; border: 0; padding: 0; font: inherit; font-size: 13px;\n  color: var(--accent-ink); text-decoration: underline; text-underline-offset: 3px;\n  cursor: pointer; margin-top: 14px;\n}\n.breakdown { margin-top: 12px; border-top: 1px solid var(--rule); padding-top: 12px; }\n.bd-group { font-size: 11px; letter-spacing: .07em; text-transform: uppercase; color: var(--ink-faint); margin: 10px 0 4px; }\n.bd-row { display: flex; justify-content: space-between; gap: 12px; font-size: 13px; padding: 2px 0; }\n.bd-row span:last-child { font-variant-numeric: tabular-nums; flex: none; }\n.bd-detail { font-size: 11.5px; color: var(--ink-faint); }\n.bd-total { display: flex; justify-content: space-between; font-size: 13px; padding: 3px 0; border-top: 1px solid var(--rule); margin-top: 10px; padding-top: 10px; }\n.bd-total--big { font-weight: 700; font-size: 15px; border: 0; padding-top: 2px; }\n\n.cta {\n  display: block; width: 100%; margin-top: 18px;\n  background: var(--accent); color: #fff; border: 1px solid var(--accent);\n  font: inherit; font-size: 15px; font-weight: 600; padding: 13px; cursor: pointer;\n  text-align: center; text-decoration: none;\n  transition: background .15s;\n}\n.cta:hover { background: var(--accent-ink); }\n.cta:disabled { background: var(--rule); border-color: var(--rule); color: var(--ink-faint); cursor: not-allowed; }\n.cta--inline { display: inline-block; width: auto; padding: 13px 28px; margin: 0; }\n.cta--ghost { background: none; color: var(--accent-ink); border-color: var(--rule); }\n.cta--ghost:hover { background: var(--cream-deep); }\n\n.quote-foot { font-size: 12px; color: var(--ink-faint); margin: 12px 0 0; line-height: 1.5; }\n.consult-actions { display: flex; gap: 12px; flex-wrap: wrap; margin: 8px 0 28px; }\n\n/* Responds to the element's own width, not the viewport's: inside a Wix page\n   the configurator can be much narrower than the screen. */\n@container fc (max-width: 940px) {\n  .config-shell { grid-template-columns: 1fr; gap: 32px; }\n  .panel-sticky { position: static; }\n  .router-cols { grid-template-columns: 1fr; gap: 20px; }\n}\n\n/* ---------------- element states ---------------- */\n.fc-loading { margin: 0; padding: 72px 24px; text-align: center; color: var(--ink-faint); font-size: 14px; }\n.alert--ok { background: var(--ok-bg); color: var(--ok-ink); }\n.submit-result { margin-top: 12px; }\n";
const TEMPLATE = "<div class=\"fc\">\n  <p id=\"fc-loading\" class=\"fc-loading\">Loading the frame shop…</p>\n\n  <div id=\"placeholder-banner\" class=\"banner\" hidden>\n    <strong id=\"banner-title\">Placeholder pricing.</strong>\n    <span id=\"banner-body\">These are invented numbers so the tool has something to show.</span>\n  </div>\n\n  <!-- ============================ SCOPE ROUTER ============================ -->\n  <section id=\"screen-router\" class=\"screen\" hidden>\n    <div class=\"wrap\">\n      <h1 class=\"display\">What are you framing?</h1>\n\n      <div class=\"router-cols\">\n        <div class=\"router-col\">\n          <h2 class=\"router-head\">Quote it online</h2>\n          <p class=\"router-blurb\">Flat, rectangular pieces. You'll get a price on this page in about a minute.</p>\n        </div>\n        <div class=\"router-col\">\n          <h2 class=\"router-head\">Bring it in to us</h2>\n          <p class=\"router-blurb\">Anything with depth, texture or sentimental risk. Owen prices these by hand — it's how they come out right.</p>\n        </div>\n        <div class=\"router-col router-col--aside\">\n          <p class=\"aside\"><span class=\"pin\">&#9679;</span> Shop 15, Design &amp; Decor Centre, Fourways</p>\n          <a class=\"link\" href=\"https://www.picframing.co.za/contact\" target=\"_blank\" rel=\"noopener\">Get directions</a>\n        </div>\n      </div>\n\n      <div class=\"tiles\" id=\"tiles-quotable\"></div>\n      <div class=\"tiles tiles--muted\" id=\"tiles-consult\"></div>\n\n      <p class=\"router-foot\">Not sure where to start? <a class=\"link\" href=\"https://www.picframing.co.za/contact\" target=\"_blank\" rel=\"noopener\">Ask us — we'll tell you straight.</a></p>\n    </div>\n  </section>\n\n  <!-- ============================ CONFIGURATOR ============================ -->\n  <section id=\"screen-config\" class=\"screen\" hidden>\n    <div class=\"config-shell\">\n\n      <!-- ---------- left: steps ---------- -->\n      <div class=\"steps\">\n        <button class=\"back-link\" id=\"btn-back\">&larr; Start over</button>\n        <h1 class=\"display display--sm\" id=\"config-title\">Frame your photo</h1>\n\n        <!-- Step 1: upload -->\n        <div class=\"step\" id=\"step-upload\">\n          <div class=\"step-head\"><span class=\"step-num\">1</span><h2>Your image</h2></div>\n          <div class=\"dropzone\" id=\"dropzone\">\n            <input type=\"file\" id=\"file-input\" accept=\"image/jpeg,image/png,image/webp,image/heic,image/heif\" hidden>\n            <div class=\"dz-inner\">\n              <div class=\"dz-icon\">&#8593;</div>\n              <p class=\"dz-title\">Drop a photo here, or <button class=\"linkbtn\" id=\"btn-browse\">browse</button></p>\n              <p class=\"dz-sub\">JPG, PNG, HEIC. We check the resolution before you pay for anything.</p>\n            </div>\n          </div>\n          <div class=\"filemeta\" id=\"filemeta\" hidden>\n            <img id=\"thumb\" alt=\"Your uploaded image\">\n            <div class=\"filemeta-text\">\n              <p class=\"fm-name\" id=\"fm-name\"></p>\n              <p class=\"fm-dims\" id=\"fm-dims\"></p>\n              <button class=\"linkbtn\" id=\"btn-replace\">Replace</button>\n            </div>\n          </div>\n        </div>\n\n        <!-- Step 2: size -->\n        <div class=\"step\" id=\"step-size\">\n          <div class=\"step-head\"><span class=\"step-num\">2</span><h2>Print size</h2></div>\n          <p class=\"step-note\" id=\"size-note\"></p>\n          <div class=\"opt-grid opt-grid--sizes\" id=\"size-options\"></div>\n        </div>\n\n        <!-- Step 3: frame -->\n        <div class=\"step\">\n          <div class=\"step-head\"><span class=\"step-num\">3</span><h2>Frame</h2></div>\n          <div class=\"opt-grid opt-grid--swatch\" id=\"moulding-options\"></div>\n        </div>\n\n        <!-- Step 4: mat -->\n        <div class=\"step\">\n          <div class=\"step-head\"><span class=\"step-num\">4</span><h2>Mat</h2></div>\n          <div class=\"seg\" id=\"mat-style\"></div>\n          <div id=\"mat-detail\" hidden>\n            <div class=\"opt-grid opt-grid--swatch\" id=\"mat1-options\"></div>\n            <div id=\"mat2-block\" hidden>\n              <p class=\"sub-label\">Under mat — the thin line of colour next to your photo</p>\n              <div class=\"opt-grid opt-grid--swatch\" id=\"mat2-options\"></div>\n            </div>\n            <div class=\"slider-row\">\n              <label for=\"border-slider\">Border width</label>\n              <input type=\"range\" id=\"border-slider\" min=\"25\" max=\"120\" step=\"5\">\n              <output id=\"border-out\"></output>\n            </div>\n            <label class=\"check\">\n              <input type=\"checkbox\" id=\"bottom-weight\">\n              <span>Weight the bottom border <em>— an old framing trick; stops it looking like it's sliding down</em></span>\n            </label>\n          </div>\n        </div>\n\n        <!-- Step 5: finishing -->\n        <div class=\"step\">\n          <div class=\"step-head\"><span class=\"step-num\">5</span><h2>Glass &amp; finishing</h2></div>\n          <div class=\"opt-list\" id=\"glazing-options\"></div>\n          <p class=\"sub-label\">Mounting</p>\n          <div class=\"opt-list\" id=\"mounting-options\"></div>\n          <p class=\"sub-label\" id=\"paper-label\">Paper</p>\n          <div class=\"opt-list\" id=\"paper-options\"></div>\n          <p class=\"sub-label\">Getting it to you</p>\n          <div class=\"opt-list\" id=\"delivery-options\"></div>\n        </div>\n      </div>\n\n      <!-- ---------- right: preview + price ---------- -->\n      <aside class=\"panel\">\n        <div class=\"panel-sticky\">\n          <div class=\"preview-stage\" id=\"preview-stage\">\n            <div class=\"pv-frame\" id=\"pv-frame\">\n              <div class=\"pv-mat\" id=\"pv-mat\">\n                <div class=\"pv-mat2\" id=\"pv-mat2\">\n                  <div class=\"pv-art\" id=\"pv-art\"><span class=\"pv-empty\">Your photo</span></div>\n                </div>\n              </div>\n            </div>\n          </div>\n          <div class=\"crop-controls\" id=\"crop-controls\" hidden>\n            <p class=\"crop-hint\">Drag the photo to reposition. Scroll to zoom.</p>\n            <div class=\"crop-row\">\n              <label for=\"crop-zoom\">Zoom</label>\n              <input type=\"range\" id=\"crop-zoom\" min=\"100\" max=\"400\" step=\"1\" value=\"100\">\n              <button class=\"linkbtn\" id=\"btn-crop-reset\">Fit</button>\n            </div>\n          </div>\n          <p class=\"dims-caption\" id=\"dims-caption\"></p>\n\n          <div class=\"quote\" id=\"quote\">\n            <div class=\"quote-head\">\n              <span class=\"quote-label\" id=\"quote-label\">Estimate</span>\n              <span class=\"quote-amount\" id=\"quote-amount\">&mdash;</span>\n            </div>\n            <p class=\"quote-sub\" id=\"quote-sub\"></p>\n            <div class=\"alerts\" id=\"alerts\"></div>\n            <button class=\"breakdown-toggle\" id=\"btn-breakdown\">See the breakdown</button>\n            <div class=\"breakdown\" id=\"breakdown\" hidden></div>\n            <button class=\"cta\" id=\"btn-request\">Request this quote</button>\n            <div class=\"alert submit-result\" id=\"submit-result\" role=\"status\" hidden></div>\n            <p class=\"quote-foot\" id=\"quote-foot\"></p>\n          </div>\n        </div>\n      </aside>\n    </div>\n  </section>\n\n  <!-- ============================ CONSULTATION ============================ -->\n  <section id=\"screen-consult\" class=\"screen\" hidden>\n    <div class=\"wrap wrap--narrow\">\n      <button class=\"back-link\" id=\"btn-back-2\">&larr; Back</button>\n      <h1 class=\"display\" id=\"consult-title\">Let's talk about this one</h1>\n      <p class=\"lede\" id=\"consult-body\"></p>\n      <div class=\"consult-actions\">\n        <a class=\"cta cta--inline\" href=\"https://wa.me/27832994208\" target=\"_blank\" rel=\"noopener\">WhatsApp us</a>\n        <a class=\"cta cta--ghost\" href=\"tel:+27114658096\">011 465 8096</a>\n      </div>\n      <p class=\"quote-foot\">Shop 15, Design &amp; Decor Centre, Fourways. Bring the piece if you can — it's much easier to price in the hand.</p>\n    </div>\n  </section>\n</div>\n";

// ---- lib/crop.js ----
/**
 * Crop geometry. Pure functions — no DOM, no state.
 *
 * The crop is modelled as a zoom factor plus a centre point in normalised
 * source coordinates, rather than as a pixel rectangle. That way it survives a
 * change of print size: the frame's aspect ratio changes, the customer's
 * intent (what they centred on, how tight they zoomed) does not.
 *
 * Nothing here re-encodes an image. The output is always a rectangle in SOURCE
 * PIXEL coordinates, which travels with the order so the workshop prints from
 * the original file.
 */

const MAX_ZOOM = 4;

const clamp = (v, lo, hi) => Math.min(Math.max(v, lo), hi);

/** Largest rectangle of `aspect` (w/h) that fits inside the source image. */
function coverCrop(pixels, aspect) {
  const imgAspect = pixels.width / pixels.height;
  return imgAspect > aspect
    ? { w: pixels.height * aspect, h: pixels.height }   // source wider: trim the sides
    : { w: pixels.width, h: pixels.width / aspect };    // source taller: trim top and bottom
}

/** Crop size at a given zoom. Zoom 1 is the cover fit; higher is tighter. */
function cropSize(pixels, aspect, zoom) {
  const cover = coverCrop(pixels, aspect);
  const z = clamp(zoom, 1, MAX_ZOOM);
  return { w: cover.w / z, h: cover.h / z };
}

/** Keep a centre point within the range that yields an in-bounds rectangle. */
function clampCentre(pixels, aspect, zoom, centre) {
  const { w, h } = cropSize(pixels, aspect, zoom);
  const halfU = w / 2 / pixels.width;
  const halfV = h / 2 / pixels.height;
  return {
    u: clamp(centre.u, halfU, 1 - halfU),
    v: clamp(centre.v, halfV, 1 - halfV),
  };
}

/** The crop rectangle, in source pixels. */
function cropRect(pixels, aspect, zoom, centre) {
  const { w, h } = cropSize(pixels, aspect, zoom);
  const c = clampCentre(pixels, aspect, zoom, centre);
  return {
    x: clamp(c.u * pixels.width - w / 2, 0, pixels.width - w),
    y: clamp(c.v * pixels.height - h / 2, 0, pixels.height - h),
    w,
    h,
  };
}

/**
 * Zoom while holding one point still — the point under the cursor, given as a
 * fraction of the displayed opening. Without this, zooming drifts away from
 * whatever the customer was looking at.
 */
function zoomAnchored(pixels, aspect, zoom, centre, anchorX, anchorY, nextZoom) {
  const before = cropRect(pixels, aspect, zoom, centre);
  const sx = before.x + anchorX * before.w;
  const sy = before.y + anchorY * before.h;

  const z = clamp(nextZoom, 1, MAX_ZOOM);
  const { w, h } = cropSize(pixels, aspect, z);

  return {
    zoom: z,
    centre: clampCentre(pixels, aspect, z, {
      u: (sx - anchorX * w + w / 2) / pixels.width,
      v: (sy - anchorY * h + h / 2) / pixels.height,
    }),
  };
}

/** Move the crop by a screen-space delta, converted back to source pixels. */
function panBy(pixels, aspect, zoom, centre, dxScreen, dyScreen, displayedWidth) {
  const rect = cropRect(pixels, aspect, zoom, centre);
  const scale = displayedWidth / rect.w;
  // Dragging the photo right reveals more of its left-hand side, hence the sign.
  return clampCentre(pixels, aspect, zoom, {
    u: (rect.x - dxScreen / scale + rect.w / 2) / pixels.width,
    v: (rect.y - dyScreen / scale + rect.h / 2) / pixels.height,
  });
}


// ---- lib/pricing.js ----
/**
 * Picframing frame configurator — pricing engine.
 *
 * Pure functions only: no DOM, no Wix APIs, no network. This file is what the
 * back-test runs against tab 7 of the intake workbook, and build.mjs copies it
 * into the Velo backend so the server can re-price requests.
 *
 * All dimensions in MILLIMETRES. All money in ZAR.
 * Rate-card figures are EXCLUDING VAT; VAT is applied once, at the end.
 *
 * Workshop rates come in two bases, because that's how the shop charges them:
 *   'frame'  a flat amount per frame (hanging hardware, corner bumpers)
 *   'm2'     per square metre of board (backing, mounting, sundries, spacers)
 */

const MM_PER_INCH = 25.4;
const MAT_COUNT = { none: 0, single: 1, double: 2 };

/* ------------------------------------------------------------------ *
 * Geometry
 *
 * Naming, front to back:
 *   art      the printed / supplied piece
 *   opening  the hole cut in the mat; smaller than the art so it cannot fall through
 *   sight    what you actually see once framed (opening + mat borders)
 *   board    mat / glass / backing cut size; extends under the frame lip
 *   rabbet   the groove in the moulding; board size + a little clearance
 *   outer    the outside of the finished frame
 * ------------------------------------------------------------------ */

function geometry(spec, rates) {
  const rules = rates.businessRules;
  const moulding = findBy(rates.mouldings, 'code', spec.mouldingCode);
  if (!moulding) throw new Error(`Unknown moulding: ${spec.mouldingCode}`);

  const hasMat = (MAT_COUNT[spec.matStyle] ?? 0) > 0;
  const overlap = hasMat ? rules.artOverlapPerSideMm : 0;

  const openingW = spec.artWidthMm - 2 * overlap;
  const openingH = spec.artHeightMm - 2 * overlap;

  const borderSides = hasMat ? spec.matBorderSidesMm : 0;
  const borderTop = borderSides;
  const borderBottom = hasMat ? (spec.matBorderBottomMm ?? borderSides) : 0;

  const sightW = openingW + 2 * borderSides;
  const sightH = openingH + borderTop + borderBottom;

  const boardW = sightW + 2 * moulding.rabbetLipMm;
  const boardH = sightH + 2 * moulding.rabbetLipMm;

  const rabbetW = boardW + rules.rabbetClearanceMm;
  const rabbetH = boardH + rules.rabbetClearanceMm;

  // A face narrower than its lip is a data problem, and the importer flags it.
  // Either way, the outside of a frame can't be smaller than its rabbet.
  const profileMm = Math.max(0, moulding.faceWidthMm - moulding.rabbetLipMm);
  const outerW = rabbetW + 2 * profileMm;
  const outerH = rabbetH + 2 * profileMm;

  // Trade chop formula: each of the four mitres eats roughly one face width of
  // stock, which is exactly the difference between the rabbet and outer
  // perimeters. Measuring the outer perimeter therefore already accounts for
  // the mitres; chopWasteMm is the shop's additional saw/setup allowance.
  const mouldingLengthMm = 2 * (outerW + outerH) + rules.chopWasteMm;

  return {
    openingW, openingH,
    sightW, sightH,
    boardW, boardH,
    rabbetW, rabbetH,
    outerW, outerH,
    mouldingLengthMm,
    outerLongSideMm: Math.max(outerW, outerH),
    boardAreaM2: (boardW * boardH) / 1e6,
    moulding,
  };
}

/* ------------------------------------------------------------------ *
 * Stock sheets
 * ------------------------------------------------------------------ */

/** Can a w x h piece be cut from a w x h sheet, turning it if need be? */
function fitsSheet(pieceW, pieceH, sheetW, sheetH) {
  if (!sheetW || !sheetH) return true;   // no sheet size on record, so don't block
  return Math.max(pieceW, pieceH) <= Math.max(sheetW, sheetH)
    && Math.min(pieceW, pieceH) <= Math.min(sheetW, sheetH);
}

/** A glazing option can be stocked in several sheet sizes, each at its own rate. */
function glazingVariants(option) {
  return option?.variants?.length ? option.variants : [option];
}

/** The cheapest sheet the glass can be cut from, or null if none is big enough. */
function pickGlazingVariant(option, boardW, boardH) {
  const longSide = Math.max(boardW, boardH);
  return glazingVariants(option)
    .filter((v) => fitsSheet(boardW, boardH, v.sheetWidthMm, v.sheetHeightMm)
      && longSide <= (v.maxLongSideMm ?? Infinity))
    .sort((a, b) => a.sellPerM2ExVat - b.sellPerM2ExVat)[0] ?? null;
}

function glazingMaxLongSide(option) {
  return Math.max(...glazingVariants(option).map((v) => v.maxLongSideMm ?? Infinity));
}

function largestVariant(option) {
  const area = (v) => (v.sheetWidthMm ?? 0) * (v.sheetHeightMm ?? 0);
  return [...glazingVariants(option)].sort((a, b) => area(b) - area(a))[0];
}

/* ------------------------------------------------------------------ *
 * Validation — the tab 1 scope gate, plus what stock sizes allow
 * ------------------------------------------------------------------ */

function validate(spec, rates) {
  const errors = [];
  const warnings = [];
  const scope = rates.scope;

  const longSide = Math.max(spec.artWidthMm, spec.artHeightMm);
  const shortSide = Math.min(spec.artWidthMm, spec.artHeightMm);
  const aspect = longSide / shortSide;

  if (longSide > scope.maxArtLongSideMm) {
    errors.push({
      code: 'OVERSIZE_LONG',
      message: `We quote online up to ${scope.maxArtLongSideMm}mm on the long side. Yours is ${Math.round(longSide)}mm.`,
      routeToConsultation: true,
    });
  }
  if (shortSide > scope.maxArtShortSideMm) {
    errors.push({
      code: 'OVERSIZE_SHORT',
      message: `We quote online up to ${scope.maxArtShortSideMm}mm on the short side. Yours is ${Math.round(shortSide)}mm.`,
      routeToConsultation: true,
    });
  }
  if (longSide < scope.minArtLongSideMm) {
    errors.push({
      code: 'UNDERSIZE',
      message: `Smallest we frame online is ${scope.minArtLongSideMm}mm on the long side.`,
      routeToConsultation: true,
    });
  }
  if (aspect > scope.maxAspectRatio + 1e-9) {
    errors.push({
      code: 'ASPECT',
      message: `That shape is more than ${scope.maxAspectRatio}:1. Panoramas need a person to look at them.`,
      routeToConsultation: true,
    });
  }

  // Everything below needs a frame we can lay out.
  const moulding = findBy(rates.mouldings, 'code', spec.mouldingCode);
  if (moulding && errors.length === 0) {
    const geo = geometry(spec, rates);

    if (geo.outerLongSideMm > moulding.maxLongSideMm) {
      errors.push({
        code: 'MOULDING_MAX',
        message: `${moulding.name} is not stable above ${moulding.maxLongSideMm}mm. Pick a heavier profile or a smaller size.`,
        routeToConsultation: false,
      });
    }

    const glazing = findBy(rates.glazing, 'code', spec.glazingCode);
    if (glazing && !pickGlazingVariant(glazing, geo.boardW, geo.boardH)) {
      errors.push({
        code: 'GLAZING_MAX',
        message: `${glazing.label} only goes up to ${glazingMaxLongSide(glazing)}mm. Choose a smaller size or different glass.`,
        routeToConsultation: false,
      });
    }

    const matCodes = new Set([spec.mat1Code, spec.mat2Code].slice(0, MAT_COUNT[spec.matStyle] ?? 0));
    for (const code of matCodes) {
      const board = findBy(rates.matBoards, 'code', code);
      if (board && !fitsSheet(geo.boardW, geo.boardH, board.sheetWidthMm, board.sheetHeightMm)) {
        errors.push({
          code: 'MAT_TOO_BIG',
          message: `${board.colourName} mat comes in ${board.sheetWidthMm} x ${board.sheetHeightMm}mm sheets, too small for this frame. Try a narrower border or another mat.`,
          routeToConsultation: false,
        });
      }
    }
  }

  if (spec.printedByUs) {
    const paper = findBy(rates.printing, 'code', spec.printPaperCode);
    if (paper?.maxPrintLongSideMm && longSide > paper.maxPrintLongSideMm) {
      errors.push({
        code: 'PRINT_MAX',
        message: `${paper.label} prints up to ${paper.maxPrintLongSideMm}mm on the long side.`,
        routeToConsultation: false,
      });
    }
    if (spec.sourcePixels) {
      const dpi = effectiveDpi(spec.sourcePixels, spec.artWidthMm, spec.artHeightMm);
      const minDpi = paper?.minDpi ?? 150;
      if (dpi < minDpi * 0.75) {
        errors.push({
          code: 'DPI_TOO_LOW',
          message: `At this size and crop you're down to ${Math.round(dpi)} DPI. It will print soft. Zoom out, or choose a smaller size.`,
          routeToConsultation: false,
        });
      } else if (dpi < minDpi) {
        warnings.push({
          code: 'DPI_MARGINAL',
          message: `${Math.round(dpi)} DPI at this size and crop — acceptable, but zooming out or dropping a size would be sharper.`,
        });
      }
    }
  }

  return { ok: errors.length === 0, errors, warnings };
}

function effectiveDpi(pixels, widthMm, heightMm) {
  const dpiW = pixels.width / (widthMm / MM_PER_INCH);
  const dpiH = pixels.height / (heightMm / MM_PER_INCH);
  return Math.min(dpiW, dpiH);
}

/** Print sizes, from the catalogue, that still hit minDpi. */
function maxSizeForImage(pixels, printSizes, minDpi) {
  return printSizes.filter((s) => effectiveDpi(pixels, s.widthMm, s.heightMm) >= minDpi);
}

/* ------------------------------------------------------------------ *
 * Price
 * ------------------------------------------------------------------ */

/** A rate charged per frame, or per square metre of board. */
function rateAmount(item, boardAreaM2) {
  const rate = item?.rateExVat ?? 0;
  return item?.basis === 'm2' ? rate * boardAreaM2 : rate;
}

function rateDetail(item, boardAreaM2) {
  return item?.basis === 'm2'
    ? `${boardAreaM2.toFixed(3)} m² at ${rand(item.rateExVat)}/m²`
    : 'per frame';
}

/** Hanging hardware, bumpers, tape. Older rate cards kept these as flat per-frame fields. */
function consumablesOf(labour) {
  return labour.consumables ?? [
    { label: 'Hanging hardware', rateExVat: labour.hangingHardwarePerFrameExVat ?? 0, basis: 'frame' },
    { label: 'Corner bumpers', rateExVat: labour.cornerBumpersPerFrameExVat ?? 0, basis: 'frame' },
    { label: 'Sundries', rateExVat: labour.sundriesPerFrameExVat ?? 0, basis: 'frame' },
  ];
}

/**
 * The full price breakdown for a spec, without checking that it's something we
 * quote online. The back-test uses this, so an out-of-scope job still gets a
 * number to compare. Customers only ever see priceQuote().
 */
function computePrice(spec, rates) {
  const rules = rates.businessRules;
  const labour = rates.labour;
  const geo = geometry(spec, rates);
  const area = geo.boardAreaM2;
  const lines = [];

  // --- Moulding -----------------------------------------------------
  const metres = geo.mouldingLengthMm / 1000;
  lines.push({
    group: 'Frame',
    label: geo.moulding.name,
    detail: `${metres.toFixed(2)} m at ${rand(geo.moulding.sellPerMExVat)}/m`,
    amount: metres * geo.moulding.sellPerMExVat,
  });

  // --- Mat board ----------------------------------------------------
  const matCount = MAT_COUNT[spec.matStyle] ?? 0;
  for (let i = 0; i < matCount; i++) {
    const code = i === 0 ? spec.mat1Code : spec.mat2Code;
    const board = findBy(rates.matBoards, 'code', code);
    if (!board) throw new Error(`Unknown mat board: ${code}`);
    const label = `${board.colourName}${matCount === 2 ? (i === 0 ? ' (top)' : ' (under)') : ''}`;

    if (board.sellPerSheetExVat != null) {
      lines.push({ group: 'Mat', label, detail: 'one sheet', amount: board.sellPerSheetExVat });
    } else {
      const sheetAreaM2 = (board.sheetWidthMm * board.sheetHeightMm) / 1e6;
      const minFraction = board.minSheetFraction ?? rules.matMinSheetFraction ?? 0;
      const chargeableM2 = Math.max(area, sheetAreaM2 * minFraction);
      lines.push({
        group: 'Mat',
        label,
        detail: `${chargeableM2.toFixed(3)} m² at ${rand(board.sellPerM2ExVat)}/m²`,
        amount: chargeableM2 * board.sellPerM2ExVat,
      });
    }
  }
  if (matCount > 0) {
    const cut = labour.matCuttingFirstOpeningExVat + (matCount - 1) * labour.matCuttingExtraOpeningExVat;
    lines.push({ group: 'Mat', label: 'Mat cutting', detail: `${matCount} opening${matCount > 1 ? 's' : ''}`, amount: cut });
  }

  // --- Glazing ------------------------------------------------------
  const glazing = findBy(rates.glazing, 'code', spec.glazingCode);
  if (!glazing) throw new Error(`Unknown glazing: ${spec.glazingCode}`);
  // Too big for every sheet is a validation error. Price it on the largest
  // sheet anyway, so a back-test still has a number to compare.
  const sheet = pickGlazingVariant(glazing, geo.boardW, geo.boardH) ?? largestVariant(glazing);
  const stockedInSizes = glazingVariants(glazing).length > 1;
  lines.push({
    group: 'Glazing',
    label: glazing.label,
    detail: `${area.toFixed(3)} m² at ${rand(sheet.sellPerM2ExVat)}/m²${stockedInSizes && sheet.type ? `, ${sheet.type}` : ''}`,
    amount: area * sheet.sellPerM2ExVat,
  });

  // --- Printing -----------------------------------------------------
  if (spec.printedByUs) {
    const paper = findBy(rates.printing, 'code', spec.printPaperCode);
    if (!paper) throw new Error(`Unknown print paper: ${spec.printPaperCode}`);
    const printAreaM2 = (spec.artWidthMm * spec.artHeightMm) / 1e6;
    const raw = printAreaM2 * paper.sellPerM2ExVat;
    const amount = Math.max(raw, paper.minChargeExVat ?? 0);
    lines.push({
      group: 'Printing',
      label: paper.label,
      detail: amount > raw ? 'minimum charge' : `${printAreaM2.toFixed(3)} m² at ${rand(paper.sellPerM2ExVat)}/m²`,
      amount,
    });
  }

  // --- Workshop -----------------------------------------------------
  const bands = labour.assemblyBands;
  const band = bands.find((b) => geo.outerLongSideMm <= b.maxLongSideMm) ?? bands[bands.length - 1];
  lines.push({ group: 'Workshop', label: 'Assembly and fitting', detail: band.label, amount: band.rateExVat });

  const backing = labour.backing ?? { rateExVat: labour.backingPerM2ExVat ?? 0, basis: 'm2' };
  lines.push({ group: 'Workshop', label: 'Backing board', detail: rateDetail(backing, area), amount: rateAmount(backing, area) });

  const mount = findBy(labour.mounting, 'id', spec.mountingId);
  if (mount && rateAmount(mount, area) > 0) {
    lines.push({ group: 'Workshop', label: mount.label, detail: rateDetail(mount, area), amount: rateAmount(mount, area) });
  }

  // With no mat, or a float mount, something has to keep the glass off the art.
  if (labour.spacer && (matCount === 0 || spec.mountingId === 'float')) {
    lines.push({ group: 'Workshop', label: 'Spacers', detail: rateDetail(labour.spacer, area), amount: rateAmount(labour.spacer, area) });
  }

  const consumables = consumablesOf(labour);
  lines.push({
    group: 'Workshop',
    label: 'Hardware and sundries',
    detail: consumables.map((c) => c.label).join(', '),
    amount: consumables.reduce((sum, c) => sum + rateAmount(c, area), 0),
  });

  // --- Packaging and delivery ---------------------------------------
  const delivery = findBy(rates.delivery, 'id', spec.deliveryId);
  if (delivery && delivery.rateExVat > 0) {
    const packaging = labour.packagingPerFrameExVat ?? 0;
    lines.push({
      group: 'Delivery',
      label: delivery.label,
      detail: packaging ? `includes ${rand(packaging)} packaging` : '',
      amount: packaging + delivery.rateExVat,
    });
  }

  // --- Totals -------------------------------------------------------
  let subtotalExVat = lines.reduce((sum, l) => sum + l.amount, 0);

  let minimumApplied = false;
  if (subtotalExVat < (rules.minOrderValueExVat ?? 0)) {
    subtotalExVat = rules.minOrderValueExVat;
    minimumApplied = true;
  }

  const vat = rules.vatRegistered ? subtotalExVat * (rules.vatRatePct / 100) : 0;
  const totalIncVat = roundTo(subtotalExVat + vat, rules.roundToNearest);

  const range = rules.quoteMode === 'range'
    ? {
        low: roundTo(totalIncVat * (1 - rules.rangePct / 100), rules.roundToNearest),
        high: roundTo(totalIncVat * (1 + rules.rangePct / 100), rules.roundToNearest),
      }
    : null;

  return {
    geometry: geo,
    lines,
    subtotalExVat,
    vat,
    totalIncVat,
    range,
    minimumApplied,
    quoteMode: rules.quoteMode,
    validityDays: rules.quoteValidityDays,
    turnaroundWorkingDays: rules.turnaroundWorkingDays ?? null,
    placeholderPricing: rates._meta?.placeholder === true,
    validatedPricing: rates._meta?.validated === true,
  };
}

function priceQuote(spec, rates) {
  const check = validate(spec, rates);
  if (!check.ok) return { ok: false, ...check };
  return { ok: true, warnings: check.warnings, ...computePrice(spec, rates) };
}

/* ------------------------------------------------------------------ *
 * Helpers
 * ------------------------------------------------------------------ */

function findBy(list, key, value) {
  return list?.find((item) => item[key] === value);
}

const rand = (n) => `R${Number(n).toFixed(2)}`;

function roundTo(value, nearest) {
  if (!nearest) return Math.round(value * 100) / 100;
  return Math.round(value / nearest) * nearest;
}

function formatZar(amount) {
  return 'R' + Math.round(amount).toLocaleString('en-ZA');
}


// ---- ui/element.js ----
const computeCropRect = cropRect;
/**
 * <frame-configurator> — the configurator as a Wix custom element.
 *
 * Runs inside a Shadow DOM, so the shop's Wix styles can't reach in and ours
 * can't leak out. It knows nothing about Wix. Data comes in through attributes
 * and goes out as DOM events, which is the whole contract a Wix custom element
 * has with its page:
 *
 *   Attributes in
 *     rate-card      JSON rate card, fetched from the backend by page code
 *     lane           open a lane directly, e.g. "digital-upload"
 *     submit-result  JSON { ok, message, requestId }: the page's reply to a request
 *
 *   Events out
 *     quote-request  detail: { lane, spec, quote, file, rateCardGenerated, createdAt }
 *
 * Velo page code can't touch the browser DOM or File objects, so anything that
 * needs the photo itself (the upload, next) has to happen in here.
 *
 * Not loaded directly: build.mjs bundles this with lib/ and the STYLES and
 * TEMPLATE strings into src/public/custom-elements/frame-configurator.js.
 */



const TAG = 'frame-configurator';
const SCREENS = ['screen-router', 'screen-config', 'screen-consult'];
const MAT_REVEAL_MM = 5;   // visible sliver of the under mat on a double
const SHOP_CONTACT = 'Please call us on 011 465 8096, or WhatsApp +27 83 299 4208.';

const onlineOnly = (list = []) => list.filter((x) => x.showOnline !== false);

/**
 * Rate-card text goes into innerHTML. It's the shop's own data, but once staff
 * edit it in the Wix CMS it's typed by hand, so escape it like any other input.
 */
const esc = (value) => String(value ?? '').replace(/[&<>"']/g, (c) => (
  { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]
));
const safeColour = (hex) => (/^#[0-9a-f]{3,8}$/i.test(hex ?? '') ? hex : '#CCCCCC');

class FrameConfigurator extends HTMLElement {
  static get observedAttributes() {
    return ['rate-card', 'lane', 'submit-result'];
  }

  constructor() {
    super();
    const root = this.attachShadow({ mode: 'open' });
    root.innerHTML = `<style>${STYLES}</style>${TEMPLATE}`;
    this.ui = mountConfigurator(this, root);
  }

  attributeChangedCallback(name, _previous, value) {
    if (value === null) return;
    if (name === 'rate-card') this.ui.setRateCard(value);
    else if (name === 'lane') this.ui.openLane(value);
    else if (name === 'submit-result') this.ui.showSubmitResult(value);
  }

  /** Load a File as if the customer had picked it. The local harness uses this. */
  loadFile(file) {
    return this.ui.acceptFile(file);
  }
}

/**
 * Builds the UI inside `root` and returns the few entry points the element
 * needs. Everything else is private to this closure, so two configurators on
 * one page wouldn't share state.
 */
function mountConfigurator(host, root) {
  const $ = (id) => root.getElementById(id);
  const all = (selector) => root.querySelectorAll(selector);

  let rates = null;
  let pendingLane = null;   // a lane asked for before the rate card arrived

  const state = {
    lane: null,            // 'digital-upload' | 'own-print' | 'document'
    printedByUs: false,
    file: null,
    pixels: null,          // { width, height }
    imageUrl: null,
    sizeId: null,
    artWidthMm: 210,
    artHeightMm: 297,
    mouldingCode: null,
    matStyle: 'single',
    mat1Code: null,
    mat2Code: null,
    matBorderSidesMm: 60,
    bottomWeighted: false,
    glazingCode: null,
    mountingId: null,
    printPaperCode: null,
    deliveryId: 'collect',
    // Crop is stored as a zoom factor plus a centre point in normalised source
    // coordinates, not as a pixel rectangle. That way it survives a change of
    // print size — the frame's aspect changes, the customer's intent doesn't.
    cropZoom: 1,
    cropCenter: { u: 0.5, v: 0.5 },
    submitting: false,
  };

  /* ---------------------------------------------------------------- *
   * Entry points
   * ---------------------------------------------------------------- */

  function setRateCard(json) {
    let next;
    try {
      next = JSON.parse(json);
    } catch {
      showLoadError("We couldn't load our price list.");
      return;
    }
    if (!next?.scope || !next?.mouldings || !next?.businessRules) {
      showLoadError('Our price list is incomplete.');
      return;
    }

    const isFirst = rates === null;
    rates = next;
    if (isFirst) applyDefaults();

    $('fc-loading').hidden = true;
    showPricingBanner(rates._meta ?? {});
    renderRouter();

    if (pendingLane) {
      const lane = pendingLane;
      pendingLane = null;
      openLane(lane);
    } else if (isFirst) {
      showScreen('screen-router', { scroll: false });
    } else if (state.lane) {
      renderAll();
    }
  }

  function openLane(id) {
    if (!rates) {
      pendingLane = id;
      return;
    }
    const lane = rates.scope.quotableJobTypes.find((t) => t.id === id);
    if (lane) startLane(lane);
  }

  function showSubmitResult(json) {
    let result;
    try { result = JSON.parse(json); } catch { result = { ok: false }; }

    state.submitting = false;
    const box = $('submit-result');
    box.hidden = false;
    box.className = `alert submit-result ${result.ok ? 'alert--ok' : 'alert--err'}`;
    box.textContent = result.message
      ?? (result.ok ? 'Sent.' : `Something went wrong sending that. ${SHOP_CONTACT}`);
    renderQuote();
  }

  function showLoadError(message) {
    SCREENS.forEach((s) => { $(s).hidden = true; });
    const el = $('fc-loading');
    el.hidden = false;
    el.textContent = `${message} ${SHOP_CONTACT}`;
  }

  /** Development banner: the prices are either invented, or real but not yet checked. */
  function showPricingBanner(meta) {
    const unchecked = !meta.placeholder && meta.validated === false;
    $('placeholder-banner').hidden = !(meta.placeholder || unchecked);
    $('banner-title').textContent = meta.placeholder ? 'Placeholder pricing.' : 'Prices not yet checked.';
    $('banner-body').textContent = meta.placeholder
      ? 'These are invented numbers so the tool has something to show.'
      : "These are the shop's real rates, but the formula hasn't been checked against enough past invoices. Don't show customers yet.";
  }

  function applyDefaults() {
    const boards = onlineOnly(rates.matBoards);
    state.mouldingCode = onlineOnly(rates.mouldings)[0]?.code ?? null;
    state.mat1Code = boards[0]?.code ?? null;
    state.mat2Code = boards[4]?.code ?? boards[0]?.code ?? null;
    state.glazingCode = onlineOnly(rates.glazing)[0]?.code ?? null;
    state.mountingId = onlineOnly(rates.labour?.mounting)[0]?.id ?? null;
    state.printPaperCode = onlineOnly(rates.printing)[0]?.code ?? null;
    state.matBorderSidesMm = rates.businessRules.defaultMatBorderMm ?? 60;
  }

  /* ---------------------------------------------------------------- *
   * Screen 1 — the scope router (tab 1 of the intake workbook)
   * ---------------------------------------------------------------- */

  function renderRouter() {
    $('tiles-quotable').innerHTML = rates.scope.quotableJobTypes.map((t) => tile(t, true)).join('');
    $('tiles-consult').innerHTML = (rates.scope.consultationJobTypes ?? []).map((t) => tile(t, false)).join('');

    all('.tile').forEach((btn) => {
      btn.addEventListener('click', () => {
        const id = btn.dataset.id;
        const quotable = rates.scope.quotableJobTypes.find((t) => t.id === id);
        if (quotable) startLane(quotable);
        else showConsultation(rates.scope.consultationJobTypes.find((t) => t.id === id));
      });
    });
  }

  function tile(t, quotable) {
    return `
      <button class="tile ${quotable ? 'tile--quotable' : ''}" data-id="${esc(t.id)}">
        <span class="tile-art"></span>
        <span class="tile-body">
          <span class="tile-name">${esc(t.label)}</span>
          <span class="tile-blurb">${esc(t.blurb)}</span>
          <span class="tile-flag">${quotable ? 'Price it now' : 'Book a consultation'}</span>
        </span>
      </button>`;
  }

  function showScreen(id, { scroll = true } = {}) {
    SCREENS.forEach((s) => { $(s).hidden = s !== id; });
    // On a Wix page the configurator is one block among many, so "back to the
    // top" means the top of the element, and only if it has scrolled out of view.
    if (scroll && host.getBoundingClientRect().top < 0) host.scrollIntoView({ block: 'start' });
  }

  /** A consultation tile, or `reason` when a quote fell outside what we price online. */
  function showConsultation({ label, blurb, reason }) {
    $('consult-title').textContent = label;
    $('consult-body').textContent = reason
      ? `${reason} Send us a photo on WhatsApp and we'll come back with a real number.`
      : `${blurb} These need eyes and hands — depth, mounting and glazing all change the price, and getting them wrong is expensive. Send us a photo on WhatsApp and we'll come back with a real number.`;
    showScreen('screen-consult');
  }

  function startLane(lane) {
    state.lane = lane.id;
    state.printedByUs = lane.printedByUs;
    $('config-title').textContent = lane.printedByUs ? 'Frame your photo' : `Frame your ${lane.label.toLowerCase()}`;
    $('step-upload').hidden = !lane.printedByUs;
    $('paper-label').hidden = !lane.printedByUs;
    $('paper-options').hidden = !lane.printedByUs;
    $('submit-result').hidden = true;
    showScreen('screen-config');   // before rendering, so the preview has a size to fit
    renderAll();
  }

  /* ---------------------------------------------------------------- *
   * Upload
   * ---------------------------------------------------------------- */

  function wireUpload() {
    const dz = $('dropzone');
    const input = $('file-input');

    dz.addEventListener('click', () => input.click());
    $('btn-browse').addEventListener('click', (e) => { e.stopPropagation(); input.click(); });
    $('btn-replace').addEventListener('click', () => input.click());

    ['dragenter', 'dragover'].forEach((type) =>
      dz.addEventListener(type, (e) => { e.preventDefault(); dz.classList.add('is-over'); }));
    ['dragleave', 'drop'].forEach((type) =>
      dz.addEventListener(type, (e) => { e.preventDefault(); dz.classList.remove('is-over'); }));

    dz.addEventListener('drop', (e) => { if (e.dataTransfer.files[0]) acceptFile(e.dataTransfer.files[0]); });
    input.addEventListener('change', (e) => {
      if (e.target.files[0]) acceptFile(e.target.files[0]);
      input.value = '';   // so picking the same file again still fires
    });
  }

  async function acceptFile(file) {
    state.file = file;
    if (state.imageUrl) URL.revokeObjectURL(state.imageUrl);
    state.imageUrl = URL.createObjectURL(file);

    state.pixels = await readPixelDimensions(state.imageUrl);
    resetCrop();
    // A landscape photo should turn the frame landscape. Without this the frame
    // keeps whatever orientation the default size had, and the size grid's DPI
    // (computed for the photo's orientation) disagrees with the live warning.
    applySizeOrientation();

    $('dropzone').hidden = true;
    $('filemeta').hidden = false;
    $('thumb').src = state.imageUrl;
    $('fm-name').textContent = file.name;
    $('fm-dims').textContent = state.pixels
      ? `${state.pixels.width} x ${state.pixels.height} px  ·  ${(file.size / 1e6).toFixed(1)} MB`
      : `${(file.size / 1e6).toFixed(1)} MB · we'll check the resolution on our side`;

    renderAll();
  }

  function readPixelDimensions(url) {
    // HEIC and some CMYK JPEGs won't decode in the browser. That's fine — we fall
    // back to letting the workshop check, rather than blocking the order.
    return new Promise((resolve) => {
      const img = new Image();
      img.onload = () => resolve({ width: img.naturalWidth, height: img.naturalHeight });
      img.onerror = () => resolve(null);
      img.src = url;
    });
  }

  /* ---------------------------------------------------------------- *
   * Controls
   * ---------------------------------------------------------------- */

  function wireControls() {
    $('btn-back').addEventListener('click', () => showScreen('screen-router'));
    $('btn-back-2').addEventListener('click', () => showScreen('screen-router'));

    $('border-slider').addEventListener('input', (e) => {
      state.matBorderSidesMm = Number(e.target.value);
      $('border-out').textContent = `${state.matBorderSidesMm} mm`;
      renderPreview();
      renderQuote();
    });

    $('bottom-weight').addEventListener('change', (e) => {
      state.bottomWeighted = e.target.checked;
      renderPreview();
      renderQuote();
    });

    $('btn-breakdown').addEventListener('click', () => {
      const bd = $('breakdown');
      bd.hidden = !bd.hidden;
      $('btn-breakdown').textContent = bd.hidden ? 'See the breakdown' : 'Hide the breakdown';
    });

    $('btn-request').addEventListener('click', onRequestClick);
  }

  /* ---------------------------------------------------------------- *
   * Rendering
   * ---------------------------------------------------------------- */

  function renderAll() {
    if (!rates) return;
    renderSizes();
    renderMouldings();
    renderMatControls();
    renderFinishing();
    renderPreview();
    renderQuote();
  }

  function renderSizes() {
    const paper = rates.printing.find((p) => p.code === state.printPaperCode);
    const minDpi = paper?.minDpi ?? 150;
    const px = state.pixels;

    $('size-note').textContent = state.printedByUs
      ? (px
          ? `Your photo is ${px.width} x ${px.height} pixels. Sizes it can't carry are greyed out — cropping in tighter will reduce what it can carry.`
          : "Upload a photo first and we'll show you which sizes it can carry.")
      : 'Measure the piece itself, not the frame you had in mind.';

    const sizes = rates.printSizes.filter((s) => {
      const long = Math.max(s.widthMm, s.heightMm);
      const short = Math.min(s.widthMm, s.heightMm);
      return long <= rates.scope.maxArtLongSideMm && short <= rates.scope.maxArtShortSideMm;
    });

    $('size-options').innerHTML = sizes.map((s) => {
      // Best case for this size: the largest crop of the right shape, un-zoomed.
      // The live warning on the quote reflects the crop actually chosen.
      const dim = orientedSize(s);
      const usable = px ? coverCrop(px, dim.w / dim.h) : null;
      const dpi = usable ? effectiveDpi({ width: usable.w, height: usable.h }, dim.w, dim.h) : null;
      const blocked = state.printedByUs && dpi !== null && dpi < minDpi * 0.75;
      const marginal = state.printedByUs && dpi !== null && dpi < minDpi && !blocked;
      const meta = dpi === null ? `${s.widthMm} x ${s.heightMm} mm`
        : blocked ? `too soft — ${Math.round(dpi)} DPI`
        : marginal ? `${Math.round(dpi)} DPI — acceptable`
        : `${s.widthMm} x ${s.heightMm} mm`;
      return `<button class="opt" data-size="${esc(s.id)}" aria-pressed="${state.sizeId === s.id}" ${blocked ? 'disabled' : ''}>
          <span class="opt-name">${esc(s.label)}</span>
          <span class="opt-meta ${marginal ? 'opt-meta--warn' : ''}">${meta}</span>
        </button>`;
    }).join('');

    bind('#size-options .opt', 'size', (id) => {
      state.sizeId = id;
      applySizeOrientation();
    });

    if (!state.sizeId && sizes.length) {
      state.sizeId = (sizes.find((s) => s.id === 'a4') ?? sizes[0]).id;
      applySizeOrientation();
      renderSizes();
    }
  }

  /** Re-derive the art dimensions from the selected size and the photo's orientation. */
  function applySizeOrientation() {
    const s = rates?.printSizes.find((x) => x.id === state.sizeId);
    if (!s) return;
    const dim = orientedSize(s);
    state.artWidthMm = dim.w;
    state.artHeightMm = dim.h;
  }

  /** Print sizes are listed portrait; turn them to match the source image. */
  function orientedSize(s) {
    const portrait = !state.pixels || state.pixels.height >= state.pixels.width;
    const long = Math.max(s.widthMm, s.heightMm);
    const short = Math.min(s.widthMm, s.heightMm);
    return portrait ? { w: short, h: long } : { w: long, h: short };
  }

  function renderMouldings() {
    $('moulding-options').innerHTML = onlineOnly(rates.mouldings).map((m) => `
      <button class="opt" data-moulding="${esc(m.code)}" aria-pressed="${state.mouldingCode === m.code}">
        <span class="swatch" style="background:${safeColour(m.hex)}"></span>
        <span class="opt-name">${esc(m.name)}</span>
        <span class="opt-meta">${esc(m.faceWidthMm)}mm wide</span>
      </button>`).join('');
    bind('#moulding-options .opt', 'moulding', (code) => { state.mouldingCode = code; });
  }

  function renderMatControls() {
    const styles = [
      { id: 'none', label: 'No mat' },
      { id: 'single', label: 'Single mat' },
      { id: 'double', label: 'Double mat' },
    ];
    $('mat-style').innerHTML = styles.map((s) =>
      `<button data-matstyle="${s.id}" aria-pressed="${state.matStyle === s.id}">${s.label}</button>`).join('');
    bind('#mat-style button', 'matstyle', (id) => { state.matStyle = id; });

    $('mat-detail').hidden = state.matStyle === 'none';
    $('mat2-block').hidden = state.matStyle !== 'double';

    const swatches = (targetId, selectedCode, key) => {
      $(targetId).innerHTML = onlineOnly(rates.matBoards).map((b) => `
        <button class="opt" data-${key}="${esc(b.code)}" aria-pressed="${selectedCode === b.code}">
          <span class="swatch" style="background:${safeColour(b.hex)}"></span>
          <span class="opt-name">${esc(b.colourName)}</span>
        </button>`).join('');
      bind(`#${targetId} .opt`, key, (code) => { state[key === 'mat1' ? 'mat1Code' : 'mat2Code'] = code; });
    };
    swatches('mat1-options', state.mat1Code, 'mat1');
    swatches('mat2-options', state.mat2Code, 'mat2');

    $('border-slider').value = state.matBorderSidesMm;
    $('border-out').textContent = `${state.matBorderSidesMm} mm`;
    $('bottom-weight').checked = state.bottomWeighted;
  }

  function renderFinishing() {
    rows('glazing-options', onlineOnly(rates.glazing), 'code', state.glazingCode, 'glazing',
      (g) => g.label, (g) => g.description);
    rows('mounting-options', onlineOnly(rates.labour.mounting), 'id', state.mountingId, 'mounting',
      (m) => m.label, (m) => m.description);
    rows('paper-options', onlineOnly(rates.printing), 'code', state.printPaperCode, 'paper',
      (p) => p.label, (p) => p.description);
    rows('delivery-options', onlineOnly(rates.delivery), 'id', state.deliveryId, 'delivery',
      (d) => d.label, (d) => d.description, (d) => (d.rateExVat === 0 ? 'Free' : null));

    bind('#glazing-options .opt-row', 'glazing', (v) => { state.glazingCode = v; });
    bind('#mounting-options .opt-row', 'mounting', (v) => { state.mountingId = v; });
    bind('#paper-options .opt-row', 'paper', (v) => { state.printPaperCode = v; });
    bind('#delivery-options .opt-row', 'delivery', (v) => { state.deliveryId = v; });
  }

  function rows(targetId, list, keyField, selected, dataKey, nameFn, descFn, priceFn) {
    $(targetId).innerHTML = list.map((item) => {
      const price = priceFn ? priceFn(item) : null;
      return `<button class="opt-row" data-${dataKey}="${esc(item[keyField])}" aria-pressed="${selected === item[keyField]}">
        <span class="opt-row-main">
          <span class="opt-row-name">${esc(nameFn(item))}</span>
          <span class="opt-row-desc">${esc(descFn(item))}</span>
        </span>
        ${price ? `<span class="opt-row-price">${esc(price)}</span>` : ''}
      </button>`;
    }).join('');
  }

  /** Wire a group of toggle buttons: set state, then re-render everything. */
  function bind(selector, dataKey, setter) {
    all(selector).forEach((btn) => {
      btn.addEventListener('click', () => {
        setter(btn.dataset[dataKey]);
        renderAll();
      });
    });
  }

  /* ---------------------------------------------------------------- *
   * Crop — DOM wiring only. The geometry lives in lib/crop.js.
   * ---------------------------------------------------------------- */

  /** Print aspect, in the orientation the frame is actually built in. */
  const artAspect = () => state.artWidthMm / state.artHeightMm;

  /** The current crop, in source pixels. Null if there's nothing to crop. */
  function cropRect() {
    if (!state.pixels) return null;
    return computeCropRect(state.pixels, artAspect(), state.cropZoom, state.cropCenter);
  }

  /**
   * Pixels the customer will actually receive. This — not the full image — is
   * what DPI must be measured against, otherwise we count pixels the crop
   * discards and quietly over-promise on the larger sizes.
   */
  function cropPixels() {
    const r = cropRect();
    return r ? { width: Math.round(r.w), height: Math.round(r.h) } : null;
  }

  function resetCrop() {
    state.cropZoom = 1;
    state.cropCenter = { u: 0.5, v: 0.5 };
    $('crop-zoom').value = 100;
  }

  function applyZoom(anchorX, anchorY, nextZoom) {
    if (!state.pixels) return;
    const next = zoomAnchored(state.pixels, artAspect(), state.cropZoom, state.cropCenter, anchorX, anchorY, nextZoom);
    state.cropZoom = next.zoom;
    state.cropCenter = next.centre;
    $('crop-zoom').value = Math.round(state.cropZoom * 100);
    renderPreview();
    renderQuote();
  }

  function wireCrop() {
    const art = $('pv-art');
    let drag = null;

    art.addEventListener('pointerdown', (e) => {
      if (!state.pixels) return;
      drag = { x: e.clientX, y: e.clientY };
      art.setPointerCapture(e.pointerId);
      art.classList.add('is-dragging');
    });

    art.addEventListener('pointermove', (e) => {
      if (!drag) return;
      state.cropCenter = panBy(
        state.pixels, artAspect(), state.cropZoom, state.cropCenter,
        e.clientX - drag.x, e.clientY - drag.y, art.clientWidth
      );
      drag = { x: e.clientX, y: e.clientY };
      renderPreview();
    });

    const endDrag = (e) => {
      if (!drag) return;
      drag = null;
      art.classList.remove('is-dragging');
      art.releasePointerCapture?.(e.pointerId);
      renderQuote();   // the crop changed, so the DPI warning may have too
    };
    art.addEventListener('pointerup', endDrag);
    art.addEventListener('pointercancel', endDrag);

    art.addEventListener('wheel', (e) => {
      if (!state.pixels) return;
      e.preventDefault();
      const box = art.getBoundingClientRect();
      applyZoom(
        (e.clientX - box.left) / box.width,
        (e.clientY - box.top) / box.height,
        state.cropZoom * (e.deltaY < 0 ? 1.08 : 1 / 1.08)
      );
    }, { passive: false });

    $('crop-zoom').addEventListener('input', (e) => applyZoom(0.5, 0.5, Number(e.target.value) / 100));
    $('btn-crop-reset').addEventListener('click', () => { resetCrop(); renderPreview(); renderQuote(); });
  }

  /* ---------------------------------------------------------------- *
   * Live preview
   * ---------------------------------------------------------------- */

  function renderPreview() {
    if (!rates) return;
    let geo;
    try { geo = geometry(buildSpec(), rates); } catch { return; }

    const stage = $('preview-stage');
    const maxW = stage.clientWidth - 48;
    const maxH = stage.clientHeight - 48;
    if (maxW <= 0 || maxH <= 0) return;   // not laid out yet, e.g. its screen is hidden
    const scale = Math.min(maxW / geo.outerW, maxH / geo.outerH);

    const moulding = rates.mouldings.find((m) => m.code === state.mouldingCode);
    const mat1 = rates.matBoards.find((b) => b.code === state.mat1Code);
    const mat2 = rates.matBoards.find((b) => b.code === state.mat2Code);

    const frame = $('pv-frame');
    frame.style.width = `${geo.outerW * scale}px`;
    frame.style.height = `${geo.outerH * scale}px`;
    frame.style.background = safeColour(moulding?.hex);

    const hasMat = state.matStyle !== 'none';
    const isDouble = state.matStyle === 'double';

    const matEl = $('pv-mat');
    matEl.style.inset = `${(moulding?.faceWidthMm ?? 0) * scale}px`;
    matEl.style.background = hasMat ? safeColour(mat1?.hex) : 'transparent';

    const sides = hasMat ? state.matBorderSidesMm : 0;
    const bottomExtra = state.bottomWeighted ? rates.businessRules.bottomWeightExtraMm : 0;
    const reveal = isDouble ? MAT_REVEAL_MM : 0;

    const mat2El = $('pv-mat2');
    mat2El.style.background = isDouble ? safeColour(mat2?.hex) : 'transparent';
    mat2El.style.top = `${(sides - reveal) * scale}px`;
    mat2El.style.left = `${(sides - reveal) * scale}px`;
    mat2El.style.right = `${(sides - reveal) * scale}px`;
    mat2El.style.bottom = `${(sides + bottomExtra - reveal) * scale}px`;

    const art = $('pv-art');
    art.style.inset = `${reveal * scale}px`;

    if (state.imageUrl) {
      art.style.backgroundImage = `url(${state.imageUrl})`;
      art.querySelector('.pv-empty')?.remove();

      const rect = cropRect();
      if (rect) {
        // Scale the whole source image so that `rect` exactly fills the opening,
        // then offset it so the rect's top-left lands on the opening's top-left.
        const factor = art.clientWidth / rect.w;
        art.style.backgroundSize = `${state.pixels.width * factor}px ${state.pixels.height * factor}px`;
        art.style.backgroundPosition = `${-rect.x * factor}px ${-rect.y * factor}px`;
        art.classList.add('is-croppable');
      } else {
        // Undecodable source (HEIC, CMYK JPEG). Show it centred and let the
        // workshop crop by hand rather than blocking the order.
        art.style.backgroundSize = 'cover';
        art.style.backgroundPosition = 'center';
        art.classList.remove('is-croppable');
      }
    }

    $('crop-controls').hidden = !(state.imageUrl && state.pixels);

    $('dims-caption').textContent =
      `Finished frame ${Math.round(geo.outerW)} x ${Math.round(geo.outerH)} mm` +
      `  ·  ${(geo.mouldingLengthMm / 1000).toFixed(2)} m of moulding`;
  }

  /* ---------------------------------------------------------------- *
   * Quote
   * ---------------------------------------------------------------- */

  function buildSpec() {
    const rules = rates.businessRules;
    return {
      artWidthMm: state.artWidthMm,
      artHeightMm: state.artHeightMm,
      matStyle: state.matStyle,
      mat1Code: state.mat1Code,
      mat2Code: state.mat2Code,
      matBorderSidesMm: state.matBorderSidesMm,
      matBorderBottomMm: state.matBorderSidesMm + (state.bottomWeighted ? rules.bottomWeightExtraMm : 0),
      mouldingCode: state.mouldingCode,
      glazingCode: state.glazingCode,
      mountingId: state.mountingId,
      printedByUs: state.printedByUs,
      printPaperCode: state.printPaperCode,
      deliveryId: state.deliveryId,
      sourcePixels: cropPixels() ?? state.pixels,
    };
  }

  function renderQuote() {
    if (!rates) return;
    const q = priceQuote(buildSpec(), rates);
    const alerts = $('alerts');
    const cta = $('btn-request');

    if (!q.ok) {
      const consult = q.errors.some((e) => e.routeToConsultation);
      $('quote-amount').textContent = '—';
      $('quote-label').textContent = 'No online price';
      $('quote-sub').textContent = '';
      $('breakdown').hidden = true;
      $('btn-breakdown').hidden = true;
      $('btn-breakdown').textContent = 'See the breakdown';
      // Out of scope goes to a person. Anything else is a choice the customer
      // can change, so the button waits for them rather than sending a dud request.
      cta.dataset.action = consult ? 'consult' : 'none';
      cta.disabled = !consult;
      cta.textContent = consult ? 'Book a consultation instead' : 'Adjust your choices to get a price';
      alerts.innerHTML = q.errors.map((e) => `<div class="alert alert--err">${esc(e.message)}</div>`).join('');
      return;
    }

    $('btn-breakdown').hidden = false;
    cta.dataset.action = 'quote';
    cta.disabled = state.submitting;
    cta.textContent = state.submitting ? 'Sending…' : 'Request this quote';

    if (q.quoteMode === 'range') {
      $('quote-label').textContent = 'Estimated range';
      $('quote-amount').textContent = `${formatZar(q.range.low)}–${formatZar(q.range.high)}`;
      $('quote-sub').textContent = 'Incl. VAT. We confirm the exact figure when we see the job.';
    } else {
      $('quote-label').textContent = 'Your price';
      $('quote-amount').textContent = formatZar(q.totalIncVat);
      $('quote-sub').textContent = `Incl. VAT. Valid ${q.validityDays} days.`;
    }

    alerts.innerHTML = [
      ...q.warnings.map((w) => `<div class="alert alert--warn">${esc(w.message)}</div>`),
      q.minimumApplied ? '<div class="alert alert--warn">Our minimum order applies to this one.</div>' : '',
    ].filter(Boolean).join('');

    renderBreakdown(q);

    $('quote-foot').textContent = [
      q.turnaroundWorkingDays ? `About ${q.turnaroundWorkingDays} working days once approved.` : '',
      state.printedByUs ? `We keep your photo for ${rates.businessRules.photoRetentionDays} days, then delete it.` : '',
    ].filter(Boolean).join(' ');
  }

  function renderBreakdown(q) {
    const groups = [...new Set(q.lines.map((l) => l.group))];
    $('breakdown').innerHTML = groups.map((g) => `
        <p class="bd-group">${esc(g)}</p>
        ${q.lines.filter((l) => l.group === g).map((l) => `
          <div class="bd-row">
            <span>${esc(l.label)}${l.detail ? `<br><span class="bd-detail">${esc(l.detail)}</span>` : ''}</span>
            <span>${formatZar(l.amount)}</span>
          </div>`).join('')}
      `).join('') + `
        <div class="bd-total"><span>Subtotal excl. VAT</span><span>${formatZar(q.subtotalExVat)}</span></div>
        <div class="bd-total" style="border:0;padding-top:2px"><span>VAT ${esc(rates.businessRules.vatRatePct)}%</span><span>${formatZar(q.vat)}</span></div>
        <div class="bd-total bd-total--big"><span>Total incl. VAT</span><span>${formatZar(q.totalIncVat)}</span></div>`;
  }

  /* ---------------------------------------------------------------- *
   * Submit — hands the request to page code as an event
   * ---------------------------------------------------------------- */

  function onRequestClick() {
    const action = $('btn-request').dataset.action;
    if (action === 'consult') {
      const q = priceQuote(buildSpec(), rates);
      const reason = q.errors?.find((e) => e.routeToConsultation)?.message;
      showConsultation({ label: "Let's talk about this one", reason });
    } else if (action === 'quote') {
      submitQuoteRequest();
    }
  }

  function submitQuoteRequest() {
    const spec = buildSpec();
    const q = priceQuote(spec, rates);
    if (!q.ok || state.submitting) return;

    state.submitting = true;
    $('submit-result').hidden = true;
    renderQuote();

    host.dispatchEvent(new CustomEvent('quote-request', {
      detail: {
        lane: state.lane,
        spec,
        quote: { totalIncVat: q.totalIncVat, range: q.range, quoteMode: q.quoteMode, lines: q.lines },
        // Metadata only. The photo itself can't travel in an event to Velo page
        // code; it'll be uploaded from inside this element instead.
        file: state.file ? {
          name: state.file.name,
          sizeBytes: state.file.size,
          type: state.file.type,
          pixels: state.pixels,
          // Source-pixel rectangle for the workshop to print from. Not a re-encode.
          crop: cropRect(),
        } : null,
        rateCardGenerated: rates._meta?.generated ?? null,
        // Echoed back in submit-result, so a repeated reply still registers as new.
        createdAt: new Date().toISOString(),
      },
    }));
  }

  /* ---------------------------------------------------------------- */

  wireUpload();
  wireControls();
  wireCrop();

  return { setRateCard, openLane, acceptFile, showSubmitResult };
}

// Last, so every const above is initialised before an existing element upgrades.
if (!customElements.get(TAG)) customElements.define(TAG, FrameConfigurator);

})();
